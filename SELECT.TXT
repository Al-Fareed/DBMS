4.1 SELECT ENAME, SAL FROM EMP WHERE SAL BETWEEN 25600 AND 35500;

4.2 SELECT ENAME FROM EMP E, DEPT D WHERE E.DEPTNO=D.DNO AND LOCATION IN('BNG','MUB','HYD');
 SELECT ENAME FROM EMP E, DEPT D WHERE E.DEPTNO=D.DNO AND (LOCATION= 'BNG' OR LOCATION='MUB' OR LOCATION='HYD');

4.3 SELECT * FROM EMP ORDER BY DEPTNO, SAL DESC;

4.4 CREATE TABLE MANAGER AS SELECT EMPNO,ENAME,JOB,DEPTNO,SAL FROM EMP;

4.5  SELECT EMPNO, ENAME FROM EMP WHERE MGRID=111;

4.6  SELECT ENAME FROM EMP WHERE ENAME LIKE '_av%j';

4.7 SELECT PRJ_NAME FROM PROJ P, DEPT D WHERE P.DNO=D.DNO AND DNAME='Marketing';


4.9  SELECT ENAME FROM EMP WHERE MGRID = (SELECT EMPNO FROM EMP WHERE ENAME='Raghu');

4.12 SELECT JOB, SUM(SAL) AS SALARY FROM EMP GROUP BY JOB;

4.15 SELECT EMPNO, ENAME, JOB FROM EMP WHERE JOB=(SELECT JOB FROM EMP WHERE ENAME='Mahesh');

4.8 SELECT UPPER(ENAME), LOWER(ENAME), INITCAP(ENAME), LENGTH(ENAME), SUBSTR(ENAME,3,2) FROM EMP;



4.11 select empno, ename from emp where extract(month from date_birth)=4;


4.14 SELECT ENAME, SAL FROM EMP WHERE SAL > (SELECT MIN(SAL) FROM EMP);


4.16 CREATE VIEW EMP_PRJ_VW AS SELECT * FROM EMP E,PROJ P WHERE DNO IN ( SELECT DNO FROM DEPT WHERE DNAME='Marketing' AND E.DEPTNO=P.DNO);

4.17 SELECT ENAME,PRJ_NAME FROM EMP_PRJ_VW;

