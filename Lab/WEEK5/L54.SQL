SET SERVEROUTPUT ON
DECLARE
CURSOR CURDISP(PAR_DEPT VARCHAR2,PAR_JOB VARCHAR2) IS
SELECT ENAME FROM EMP
WHERE DEPTNO=PAR_DEPT AND JOB=PAR_JOB;
BEGIN
FOR I IN CURDISP('&PAR_DEPT','&PAR_JOB')
LOOP
DBMS_OUTPUT.PUT_LINE(I.ENAME);
END LOOP;
END;
/
